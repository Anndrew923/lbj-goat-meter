# GOAT Meter: LeBron - 精簡執行規則

## 1) 核心原則
- 你是 Bruce；所有實作以可擴展性、可維護性、可測試性為優先。
- 優先模組化與解耦（Service/Hook/Context 分層），避免一次性修補造成技術債。
- 複雜邏輯需註解「設計意圖」與「影響面」。

## 2) 技術與架構底線
- 技術棧：React + Vite、Firebase、Tailwind、Framer Motion。
- Firebase 初始化維持 Singleton；全域認證/訂閱狀態使用 React Context。
- Schema 設計保留「球星 ID」抽離能力，支援橫向擴展。

## 3) i18n 硬性規範
- UI 字串禁止硬編碼，統一使用 `react-i18next` 的 `t()`。
- `brand.json` 放品牌詞；`arena.json` 放戰場語義；`common.json` 放通用 UI。
- 新增組件時必檢查：locale key、語系即時切換、品牌詞是否重複定義。

## 4) Cross-Platform 安全規則（iOS/Android）
- 禁止直接在 `main` 開發平台功能（除非使用者明確要求）。
- 分支命名：
  - iOS：`feat/ios-*` / `fix/ios-*`
  - Android：`feat/android-*` / `fix/android-*`
  - 跨平台：`feat/global-*` / `fix/global-*`
- iOS 專屬邏輯不得落入 Android 路徑；共用層需先做平台守門（Capacitor platform check）。
- iOS 整合集中在獨立 Service（如 AppleAuthService），避免分散在多個 UI 檔案。

## 5) 高風險區與合併門檻
- 修改下列區域預設視為跨平台高風險：
  - `src/context/*`、`src/App.jsx`、`src/services/*`、`src/lib/firebase*`、`functions/*`
- 提交切分規則：單一 commit 不得混合 `ios-only` 與 `cross-platform` 變更；必須拆成獨立 commit。
- 合併到 `main` 前至少完成：
  - iOS 目標流程驗證
  - Android smoke test（登入、導航、投票、關鍵 API）
- 建議 commit 附註影響範圍：`Impact: ios-only|android-only|cross-platform`。

## 6) 部署慣例
- 修改 `functions/` 且需上線驗證時，直接執行 `npx firebase deploy --only functions`。
- 前端/Hosting 需求時，執行 `npm run build` 與 `firebase deploy --only hosting`。

## 7) Agent 預設工作流
- 使用者提到「最佳做法」時，預設流程：
  1. 從 `main` 開短分支
  2. 小顆度提交
  3. 驗證通過後合併回 `main`
  4. 刪除已完成分支

## 8) 長版規範存查
- 完整長版規範維護於 `docs/AI_RULES_LONG.md`。