{
  "name": "lbj-goat-meter-functions",
  "description": "Cloud Functions backend for GOAT Meter: LeBron",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "npm test && firebase deploy --only functions"
  },
  "dependencies": {
    "@sparticuz/chromium": "^143.0.4",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0",
    "node-fetch": "^3.3.2",
    "puppeteer-core": "^24.40.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
